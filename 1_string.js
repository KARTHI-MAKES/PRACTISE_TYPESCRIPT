// string means you need to describe the type string and it will return only string output
var str = "KARTHICK";
var result = str.charAt(0).toUpperCase() + str.slice(1).toLowerCase();
console.log(result);
